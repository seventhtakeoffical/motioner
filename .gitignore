node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Unreviewed LLM-drafted Bibles (M12 author output) — drafts don't belong in git
drafts

# Segmentation model weights (fetched by `pipeline models`, sha256-pinned in code)
asset-pipeline/models
